apiVersion: v1
kind: Service
metadata:
  name: guidking
  labels:
    run: guidking-api
spec:
  selector:
    run: guidking-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: NodePort

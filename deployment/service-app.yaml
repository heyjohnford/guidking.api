apiVersion: v1
kind: Service
metadata:
  name: guidking
  labels:
    name: guidking-api
spec:
  selector:
    name: guidking-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: NodePort
